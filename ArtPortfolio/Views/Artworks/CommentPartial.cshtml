@model CommentViewModel

<div class="p-3">
    <div class="container">
        <h5 class="card-title"><small>from: </small>@this.User.GetUsername()</h5>
        <p class="card-text">@Model.Content</p>
    </div>
</div>